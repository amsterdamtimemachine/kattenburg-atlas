name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out atlas content
        uses: actions/checkout@v4
        with:
          path: atlas

      - name: Prepare slides app
        working-directory: atlas
        env:
          SLIDES_APP_DIR: ${{ github.workspace }}/slides-app
          SLIDES_REF: ${{ vars.SLIDES_REF || 'main' }}
        run: bash scripts/prepare-slides-app.sh

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: slides-app/pnpm-lock.yaml

      - name: Install dependencies
        working-directory: slides-app
        run: pnpm install --frozen-lockfile

      - name: Check public configuration
        env:
          PUBLIC_PROTOMAPS_KEY: ${{ vars.PUBLIC_PROTOMAPS_KEY }}
        run: |
          if [ -z "$PUBLIC_PROTOMAPS_KEY" ]; then
            echo "::warning title=Missing PUBLIC_PROTOMAPS_KEY::Set PUBLIC_PROTOMAPS_KEY as a repository variable to enable Protomaps tiles."
          fi

      - name: Build static site
        working-directory: slides-app
        env:
          PUBLIC_URL: ${{ vars.PUBLIC_URL || format('/{0}', github.event.repository.name) }}
          PUBLIC_PROTOMAPS_KEY: ${{ vars.PUBLIC_PROTOMAPS_KEY }}
          PUBLIC_SLIDES_SINGLE_PROJECT_ROOT: ${{ vars.PUBLIC_SLIDES_SINGLE_PROJECT_ROOT }}
        run: pnpm build

      - name: Disable Jekyll processing
        working-directory: slides-app
        run: touch build/.nojekyll

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: slides-app/build

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
