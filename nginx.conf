server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SvelteKit exports chapter routes as files such as /1800-1900.html.
    location / {
        try_files $uri $uri.html $uri/ =404;
    }
}
